/**
 * Main entry point for Google Cloud Functions
 * Each function is deployed independently.
 *
 * To deploy a function:
 * npm run deploy:getTickers
 *
 * To test locally:
 * npm run dev
 */

import "./functions/getTickers/index.js";
